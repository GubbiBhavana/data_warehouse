resources:
  pipelines:
    employee_pipeline:
      name: employee_pipeline
      libraries:
        - glob:
            include: ../employee/transformations/**
      schema: ${var.schema}
      channel: CURRENT
      catalog: ${var.catalog}
      serverless: true