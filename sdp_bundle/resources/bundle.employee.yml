resources:
  pipelines:
    employee_pipeline:
      name: employee_pipeline
      libraries:
        - glob:
            include: ../employee/transformations/*
      schema: ${var.schema}
      continuous: false
      development: false
      photon: true
      channel: CURRENT
      catalog: ${var.catalog}
      serverless: true
      root_path: ../employee/transformations
