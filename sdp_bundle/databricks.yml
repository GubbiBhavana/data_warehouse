# Declarative Automation Bundle configuration, more info here: https://docs.databricks.com/en/dev-tools/bundles/settings.html
bundle:
  name: "databricks"

include:
  - resources/*.yml

variables:
  catalog:
    default: bhavana_practise
  schema:
    default: sdp
    
resources:
  jobs:
    employee_mv_job:
      name: employee_mv_job 
      tasks:
        - task_key: run_employee_pipeline
          pipeline_task: 
            pipeline_id: ${resources.pipelines.employee_pipeline.id}
            pipeline_parameters:
              id: "{{job.parameters.id}}"
              name: "{{job.parameters.name}}"
      parameters:
        - name: id
          default: "<MISSING_FROM_JOB>"
        - name: name
          default: "<MISSING_FROM_JOB>"
      queue:
        enabled: true